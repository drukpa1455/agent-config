interface:
  display_name: "Genius"
  short_description: "Apply a revision-bound Tinygrad engineering lens"
  default_prompt: "Apply the Tinygrad decision card to this problem, cite pinned mechanics when decisive, and transfer invariants without simulating personalities or copying compiler machinery."
policy:
  allow_implicit_invocation: false
