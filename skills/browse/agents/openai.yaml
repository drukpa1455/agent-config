interface:
  display_name: "Browse"
  short_description: "Control isolated or authenticated browsers"
  default_prompt: "Use $browse to complete this interactive browser task."
policy:
  allow_implicit_invocation: true
